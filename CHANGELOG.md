# 更新记录

## 2026-09-10：上下文导航、错题复测与回归验证

### 错题复测闭环修复

- 修复完成订正后“再测一道”无法稳定触发的问题；
- 复测新会话不再从自身聊天历史中反查原错题，而是保存原错题 `referenceQuestion`，并通过结构化 `exercise_reference` 显式传给后端；
- 后端对参照题重新执行知识分析，并在 `request_kind="exercise"` 下强制进入练习出题模式；
- 复测生成阶段保存 `generated_question` 与隐藏的 `generated_answer`；
- 学生提交复测答案时，将“本次复测题 + 隐藏参考答案（如有）+ 学生作答”作为独立核验上下文，避免误判原错题或其他历史题；
- 复测结果继续记录正确、错误与未能判断状态，并同步更新错题复测统计。

### 历史题上下文导航重构

- 扩展绝对题号识别：支持“第一题”“第 2 题”“第三道大题”等表达；
- 增加倒序题号：支持“倒数第一题”“倒数第二题”；
- 增加前后导航：支持“上一道题”“下一道题”“再上一题”“再下一题”“往前/往后 N 道题”；
- 增加边界与组合导航：支持“最开始那道题”“最后一道题”“当前题”“第 2 题的下一题”等；
- 保留主题指代能力，如“集合那道题”“数论那道题”；
- 明确区分会话题号与当前大题内部小问：“第 2 小题”“第 2 问”不再触发历史第 2 题导航；
- 前端在请求发出前写入目标题指纹与题目锚点，后端优先使用明确目标，避免相对导航被重复计算；
- 显式导航目标不存在时不再静默回落到当前题，而是返回可理解的定位失败提示；
- 对“下一题”的导航/出题歧义增加专门判断，避免“下一道题不太会”被误判为生成新题。

### 120 项系统自测

- 完成 120 项自测并全部通过；
- 6 个测试分组各 20 项：核心教学、OCR 多模态、上下文与错题闭环、前端与 PDF、部署稳定性、知识图谱专项；
- 优先级统计：P0 55 项、P1 59 项、P2 6 项；
- 测试记录中 120 项实际结果均为“与预期一致”，状态均为“通过”。

## 部署与运行信息更新

- 补充项目早期 Render 部署记录，并明确当前部署平台为 Zeabur；
- 补充当前 Tencent Seoul 2C 4GB 服务器信息与 US$4/月服务器费用；
- 补充截至 2026-09-10 的 DeepSeek API 累计实际支出 ¥101.56。

## 界面修正

- 统一弹窗关闭控件为红色圆点样式；
- 错题本搜索清除按钮改为输入内容后才显示，并修正垂直居中与右侧间距。

## 上下文容量调整

代码范围变化：

- `app.py`：单次消息上限由 16 调整为 32；
- `ai.py`：AI 历史上限调整为 32，并加入 60,000 字符历史预算；
- `static/script.js`：自然语言回退上下文调整为最近 12 条消息。

核心目标：在保留显式题目状态管理的基础上，扩大普通连续对话的近期上下文容量，同时控制历史噪声与请求规模。

## 数学知识审校与内容扩展

代码范围变化：

- 修正“邻接表”与“邻接矩阵”的概念映射；
- 修正“原像”与“逆函数”的概念映射；
- 将桥/割边从树专属语义中移出并归入连通性；
- 知识图谱扩展为 10 个模块、64 个节点；
- 加入初等数论、证明与归纳、关系运算、前束范式、带权图、割点与割边、根树等节点；
- `prerequisites` 明确为建议学习前置/依赖关系；
- 前端关键 CDN 库固定具体版本。
